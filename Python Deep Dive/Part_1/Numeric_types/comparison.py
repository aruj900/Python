print( 3 in [1,2,3])

print([1,2] is [1,2]) # Due to different memory address

# Comparison doesn't work with complex numbers

print(3 < 2 < 1/0)

#This is due to shortcircuiting as 3 < 2 and 2 < 1/0 so second is never evaluated

'A' < 'a' # Capital is less than lowercase

 