print(type(100))

import sys
print(sys.getsizeof(0))

#as we have bigger numbers it will take more memory and operations will also be slower


#operations on integers

# Floor of an integer is the largest integer <= a // is floor (watch out for negtive numbers)

print(type(1+1))

type(2/3)
import math
print(math.floor(3.15))

print(math.floor(-3.00001))


a = 13
b = 4
print('{0}/{1} = {2}'.format(a,b,a/b))
print('{0}//{1} = {2}'.format(a,b,a//b))
print('{0}%{1} = {2}'.format(a,b,a%b))
